#ifndef DATATYPE_LEN_H
#define DATATYPE_LEN_H


/* get the length of a double */
size_t double_len(double value);

/* get the length of a size_t (aka unsigned int)*/
size_t size_t_len(size_t value);

#endif